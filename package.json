{
  "name": "texas-holdem-score",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --port 3000 --do ROOM=Room --persist-to=.wrangler/state",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  },
  "dependencies": {
    "ws": "^8.20.0"
  }
}
